{
  "name": "nuta",
  "version": "0.0.1",
  "description": "Open-source music in your terminal.",
  "type": "module",
  "private": true,
  "bin": {
    "nuta": "src/index.ts"
  },
  "scripts": {
    "dev": "bun run scripts/run.ts",
    "dev:apple": "bun run scripts/dev-apple.ts",
    "playback:authorize": "bun --no-env-file run scripts/authorize-apple-playback.ts",
    "playback:probe": "bun --no-env-file run scripts/probe-apple-playback.ts",
    "start": "bun run scripts/run.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "license": "MIT",
  "dependencies": {
    "@opentui/core": "^0.5.8",
    "puppeteer-core": "^25.9.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
